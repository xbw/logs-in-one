<?xml version="1.0" encoding="UTF-8"?>
<!--日志级别以及优先级排序: OFF > FATAL > ERROR > WARN > INFO > DEBUG > TRACE > ALL -->
<!--Configuration后面的status，这个用于设置log4j2自身内部的信息输出，可以不设置，当设置成trace时，你会看到log4j2内部各种详细输出 -->
<!--monitorInterval：Log4j能够自动检测修改配置 文件和重新配置本身，设置间隔秒数 -->
<configuration status="WARN" monitorInterval="30">
	<!--先定义所有的appender -->
	<appenders>
		<!--这个输出控制台的配置 -->
		<console name="Console" target="SYSTEM_OUT">
			<!--输出日志的格式 -->
			<PatternLayout pattern="%d{HH:mm:ss.SSS} [%thread] %-5level %logger - %msg%n" />
		</console>


		<!-- 按年份-月份建立的文件夹下面并进行压缩，作为存档 -->
		<!-- filePattern="../logs/$${date:yyyy-MM}/xbw-log4j2-default-%d{yyyy-MM-dd}-%i.log" -->
		<RollingFile name="Rolling" fileName="../logs/xbw-log4j2-default.log"
			filePattern="../logs/xbw-log4j2-default-%d{yyyy-MM-dd}-%i.log">
			<PatternLayout pattern="%d{HH:mm:ss.SSS} [%thread] %-5level %logger - %msg%n" />
			<Policies>
				<TimeBasedTriggeringPolicy />
				<SizeBasedTriggeringPolicy size="100 MB" />
			</Policies>
			<!-- 最大保存文件数 -->
			<DefaultRolloverStrategy max="60" />
		</RollingFile>
	</appenders>


	<!--然后定义logger，只有定义了logger并引入的appender，appender才会生效 -->
	<loggers>
		<root level="ALL">
			<appender-ref ref="Console" />
			<appender-ref ref="Rolling" />
		</root>
	</loggers>
</configuration>